import { prisma } from '@prisma/client'
import { mangas }from '@prisma/client'

import { mangaType } from '../services/mangaService'

// export async const MangaExists(mangaName: string) {

//     const manga = await prisma.mangas.findMany();
    
//   })
// }
